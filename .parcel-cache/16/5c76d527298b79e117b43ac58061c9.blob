// animated form by devEd
function $6c4ac405b0d8bd70988dae4c669d60f7$var$animatedForm() {
    const arrows = document.querySelectorAll('.fa-arrow-down');
    arrows.forEach((arrow)=>{
        arrow.addEventListener('click', ()=>{
            const input = arrow.previousElementSibling;
            const parent = arrow.parentElement;
            const nextForm = parent.nextElementSibling;
            //  Check for validation
            if (input.type === 'text' && $6c4ac405b0d8bd70988dae4c669d60f7$var$validateUser(input)) {
                // progress indicator
                gsap.to('#namePass', {
                    duration: 0.5,
                    backgroundColor: '#23c552'
                });
                gsap.from(nextForm, {
                    duration: 0.5,
                    y: 50,
                    ease: 'sine'
                });
                $6c4ac405b0d8bd70988dae4c669d60f7$var$nextField(parent, nextForm);
            } else if (input.type === 'email' && $6c4ac405b0d8bd70988dae4c669d60f7$var$validateEmail(input)) {
                // progress indicator
                gsap.to('#emailPass', {
                    duration: 0.5,
                    backgroundColor: '#23c552'
                });
                gsap.from(nextForm, {
                    duration: 0.5,
                    y: 50,
                    ease: 'sine'
                });
                // input.addEventListener('keypress', e => {
                //     if (e.key == 'g') {
                //         console.log(e)
                //         nextField(parent, nextForm)
                //     }
                //     console.log(e)
                // })
                $6c4ac405b0d8bd70988dae4c669d60f7$var$nextField(parent, nextForm);
            } else if (input.tagName === 'TEXTAREA' && $6c4ac405b0d8bd70988dae4c669d60f7$var$validateMessage(input)) {
                // progress indicator
                gsap.to('#messagePass', {
                    duration: 0.5,
                    backgroundColor: '#23c552'
                });
                // next field animation
                gsap.from(nextForm, {
                    duration: 0.5,
                    y: 50,
                    ease: 'sine'
                });
                $6c4ac405b0d8bd70988dae4c669d60f7$var$nextField(parent, nextForm);
            } else input.type;
        // get rid of animation
        // parent.addEventListener('animationend', () => {
        //     parent.style.animation = ''
        // })
        });
    });
}
function $6c4ac405b0d8bd70988dae4c669d60f7$var$validateUser(user) {
    if (user.value.length < 6) {
        console.log('not enough characters');
        $6c4ac405b0d8bd70988dae4c669d60f7$var$error('#F84F31', user);
    } else // error('#23c552', user);
    return true;
}
function $6c4ac405b0d8bd70988dae4c669d60f7$var$validateMessage(message) {
    if (message.value.length < 6) {
        console.log('not enough characters');
        $6c4ac405b0d8bd70988dae4c669d60f7$var$error('#F84F31', message);
    } else // error('#23c552', message);
    return true;
}
function $6c4ac405b0d8bd70988dae4c669d60f7$var$validateEmail(email) {
    const validation = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (validation.test(email.value)) return true;
    else $6c4ac405b0d8bd70988dae4c669d60f7$var$error('#F84F31', email);
}
document.querySelector('.contact-form-parent form').addEventListener('submit', $6c4ac405b0d8bd70988dae4c669d60f7$var$submitForm);
function $6c4ac405b0d8bd70988dae4c669d60f7$var$submitForm(e) {
    e.preventDefault();
    gsap.to('#submitPass', {
        duration: 0.5,
        backgroundColor: '#23c552'
    });
    // next field animation
    gsap.from('.field-finish', {
        duration: 0.5,
        y: 50,
        ease: 'sine'
    });
    // gsap.to('.contact-form-wrapper', {
    //     duration: 1.5,
    //     backgroundColor: 'navy',
    //     ease: 'sine'
    // });
    document.querySelector('form').reset();
    document.querySelector('.field-submit').classList.add('inactive');
    document.querySelector('.field-submit').classList.remove('active');
    document.querySelector('.field-finish').classList.add('active');
}
function $6c4ac405b0d8bd70988dae4c669d60f7$var$nextField(parent, nextForm) {
    parent.classList.add('inactive');
    parent.classList.remove('active');
    nextForm.classList.add('active');
}
function $6c4ac405b0d8bd70988dae4c669d60f7$var$error(color, e) {
    let parent = e.parentElement;
    // gsap.to('', {
    //     duration: 0.5,
    //     backgroundColor: 'navy'
    // });
    if (parent.classList.contains('field-name')) {
        gsap.to('#namePass', {
            duration: 0.5,
            backgroundColor: color
        });
        gsap.from('#namePass', {
            duration: 0.5,
            y: -20,
            ease: 'bounce'
        });
    } else if (parent.classList.contains('field-email')) {
        gsap.to('#emailPass', {
            duration: 0.5,
            backgroundColor: color
        });
        gsap.from('#emailPass', {
            duration: 0.5,
            y: -20,
            ease: 'bounce'
        });
    } else if (parent.classList.contains('field-message')) {
        gsap.to('#messagePass', {
            duration: 0.5,
            backgroundColor: color
        });
        gsap.from('#messagePass', {
            duration: 0.5,
            y: -20,
            ease: 'bounce'
        });
    }
}
$6c4ac405b0d8bd70988dae4c669d60f7$var$animatedForm();
document.querySelector('.field-email input').addEventListener('keypress', (e)=>{
    if (e.key == 'Enter') e.preventDefault();
});
